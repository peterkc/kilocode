# Kilo Code Research Repository

Orphan branch for research and exploration artifacts (peterkc fork).

## Relationship

- Orphan branch `research` of peterkc/kilo
- Mounted as worktree at `.agents/research/` in main repo
- Excluded via .gitignore (committed to origin/dev)

## Workflow

```bash
git status
git add <files>
git commit -m "..."
git push origin research
```
