# Tracer Test Rule

If this rule is active, declare TRACER_ACTIVE at the start of your next response.

This tests whether additionalDirectories loads rules from secondary projects.
Set CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1 and add /Volumes/atlas/kilocode
as an additional directory to enable.
